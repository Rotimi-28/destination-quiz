<div class="Ques-sec">
{{#each questions as |question|}}
<ul>
    {{question.question}}
    {{#each question.options as |option|}}
    <li>{{option.option}}</li>
    {{/each}}
</ul>
{{/each}}

<button type="submit" class="btn btn-primary" href="/login">Sign up to find out your destination!</button>

<script src="./public/assets/js/login.js"></script>